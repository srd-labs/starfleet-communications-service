apiVersion: apps/v1
kind: Deployment
metadata:
  name: starfleet-communications-service
  labels:
    app: starfleet-communications-service
spec:

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1

  selector:
    matchLabels:
      app: starfleet-communications-service

  template:
    metadata:
      labels:
        app: starfleet-communications-service

    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: starfleet-communications-service
      containers:
        - name: communications-service
          image: us-central1-docker.pkg.dev/starfleet-gke-platform-lab/starfleet-apps/starfleet-communications-service:v0.1.0

          ports:
            - containerPort: 8080

          env:
            - name: RELEASE_VERSION
              value: "v0.1.0"

            - name: COMMIT_ID
              value: "189351a"

          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
